import CustomSafeAreaView from "@/components/CustomSafeAreaView";
import { Text } from 'react-native';

export default function Regulations() {

    return(
        <CustomSafeAreaView>
            <Text>룰로된 규정 제공 페이지</Text>
        </CustomSafeAreaView>
    );
}